import { defineRoot } from "{{utilsPackage}}";

export const root = defineRoot({
  brief: "{{description}}",
  // Uncomment to add custom context:
  // createContext: () => ({
  //   cwd: process.cwd(),
  // }),
  // Uncomment for route aliases:
  // aliases: { pkg: "package" },
});

export type AppContext = typeof root.Context;
